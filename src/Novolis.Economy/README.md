# Novolis.Economy

Core primitives for headless economic simulation: strong IDs, `Money` / `Quantity` / `Percentage`, discrete simulation time, command/event/projection markers, metric explanations, and seeded RNG.

```bash
dotnet add package Novolis.Economy
```

See [docs/design.md](../../docs/design.md).
